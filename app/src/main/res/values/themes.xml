<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MusicApplication" parent="Base.Theme.MusicApplication" />
    <style name="SplashScreenTheme" parent="Theme.Material3.Light">
        <item name="android:windowBackground">@drawable/splash_screen_layer_list</item>
    </style>
</resources>